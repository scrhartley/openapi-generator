openapi: 3.0.0
info:
  version: 1.0.0
  title: Demo
servers:
  - url: 'http://localhost'
paths:
  '/{ids}':
    get:
      parameters:
        - name: ids
          in: path
          required: true
          schema:
            type: array
            items:
              type: string
          style: simple
      responses:
        200:
          description: Successful operation
